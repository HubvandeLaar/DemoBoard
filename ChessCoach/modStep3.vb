Imports ChessMaterials
Imports ChessEngine

Module modStep3

    Public Function Step3Errors(pBoardBefore As ChessBoard, pMove As BoardMove, pBoardAfter As ChessBoard, pResult As EngineResults) As String
        If pResult.Score < -100 Then

        End If
        Return ""
    End Function

End Module
